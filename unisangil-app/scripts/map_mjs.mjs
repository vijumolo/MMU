import fs from 'fs'; import { PDFExtract } from 'pdf.js-extract'; const extract = new PDFExtract(); extract.extract('../Diplomas/DIPLOMAS UNISANGIL.pdf', {}, (err, data) => { if (err) return console.log(err); const dorsalToPage = {}; data.pages.forEach(page => { const pageText = page.content.map(c => c.str).join(' '); const match = pageText.match(/DORSAL No\.\s*(\d+)/) || pageText.match(/No\.\s*(\d+)/) || pageText.match(/DORSAL No\.DORSAL No\.\s*(\d+)/); if(match) { dorsalToPage[match[1]] = page.pageInfo.num; } }); const jsonData = JSON.parse(fs.readFileSync('./public/data.json')); jsonData.forEach(r => { if(dorsalToPage[r.dorsal]) { r.diplomaPage = dorsalToPage[r.dorsal]; } else { r.diplomaPage = 1; } }); fs.writeFileSync('./public/data.json', JSON.stringify(jsonData, null, 2)); console.log('Mapped ' + Object.keys(dorsalToPage).length + ' pages!'); });
