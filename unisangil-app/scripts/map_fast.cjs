const fs = require('fs'); const { PDFParse } = require('pdf-parse'); async function run() { const dataBuffer = fs.readFileSync('../Diplomas/DIPLOMAS UNISANGIL.pdf'); const data = await PDFParse(dataBuffer); const pages = data.text.split('----------------Page'); const dorsalToPage = {}; pages.forEach((pageText, idx) => { const match = pageText.match(/DORSAL No\.\s*(\d+)/) || pageText.match(/No\.\s*(\d+)/) || pageText.match(/DORSAL No\.DORSAL No\.\s*(\d+)/); if(match) { dorsalToPage[match[1]] = idx; } }); const jsonData = JSON.parse(fs.readFileSync('./public/data.json')); jsonData.forEach(r => { if(dorsalToPage[r.dorsal]) { r.diplomaPage = dorsalToPage[r.dorsal]; } else { r.diplomaPage = 1; } }); fs.writeFileSync('./public/data.json', JSON.stringify(jsonData, null, 2)); console.log('Mapped ' + Object.keys(dorsalToPage).length + ' pages!'); } run().catch(console.error);
